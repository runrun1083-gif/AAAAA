# DEFINITION: Unified Single-File Development Protocol (Development-Centric)

## 1. 物理的配置の優先順位（Physical Ordering Priority）
AIはコードを生成・追加する際、ファイルの上から順に以下の「階層（Tier）」を厳守して配置せよ。いかなる場合も下のTierが上のTierより先に定義されることを禁ずる。

- **Tier 1: SYSTEM & ENV** (sys.setrecursionlimit, imports)
- **Tier 2: STATIC DATA** (Constants, Large Dictionaries, Resource Strings)
- **Tier 3: ARCHS & BASES** (Interfaces, Abstract Classes, Base Classes)
- **Tier 4: LOGIC & IMPLS** (Derived Classes, Primary Functions)
- **Tier 5: INJECTION & BINDING** (Monkey Patching for Circular Refs, Method Assignments)
- **Tier 6: EXECUTABLES** (Encapsulated Main Functions, Final Entry Point)

---

## 2. 名前空間のハンドリング（Namespace Handling）
「足し算」による衝突を避けるため、すべてのシンボルに「仮想的なモジュール境界」を物理的に刻印する。

1. **Prefix-Standard**:
   すべての定義は `{Domain}_{Name}` 形式とする。
   (例: `Auth_User`, `Storage_User` など、機能単位のドメインを接頭辞にする)
2. **Context Segregation**:
   グローバル変数は `{Domain}_State` クラスにカプセル化し、`{Domain}_State.value` としてアクセスする。生変数のトップレベル配置は禁止。
3. **Internal-Call Normalization**:
   本来 `import` が必要な別領域の機能呼び出しは、すべて上記修飾名を用いた「直接参照」として記述する。

---

## 3. 逐次評価整合性（Sequential Evaluation Rule）
Pythonのインタープリタが上から順に読み込む際の `NameError` を物理的に排除する。

- **Dependency Scan**: 
  新しい関数やクラスを追加する前に、その中で参照しているすべての型・定数・基底クラスが、現在の行より「上」にあることを確認せよ。
- **Top-Down Decorators**:
  デコレータ関数は、それを利用するターゲットよりも物理的に上方に配置せよ。
- **Late-Binding**:
  循環参照（AがBを使い、BがAを使う）が発生する場合は、メソッドの中身を関数としてTier 4で定義し、Tier 5で `Class.method = func` として結合せよ。

---

## 4. 開発時運用ルール（Execution Instruction）
- **No Fragmentation**: 
  「一部のコード」ではなく、常に「この定義に基づいた完全な単一ファイル」として出力せよ。
- **Total Resolution**: 
  `getattr` 等の動的アクセスに使用される文字列も、上記 `Prefix-Standard` に基づいて静的に解決・置換して記述せよ。
- **Atomic Modification**:
  機能を追加する際は、適切な Tier 位置に差し込み、ファイル全体の整合性を再計算した状態で出力せよ。