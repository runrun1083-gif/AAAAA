# インフラ設計ガイドライン

## 1. 開発・実行環境 (Layer 0) - 定義：完全分離型ハイブリッド
本プロジェクトは、システム環境(Homebrew/macOS)とプロジェクト環境(.venv)を厳格に分離する。

### 1.1 コア・ランタイム
- **Python Manager**: `uv` (Universal Python Project Management)
  - **役割**: Pythonバージョンの管理、仮想環境の作成、パッケージの同期。
  - **正義**: `pyproject.toml` と `uv.lock` のみが真実である。
- **Python Runtime**: `.venv/bin/python` (Python 3.12.12以上)
  - **原則**: システム標準 (`/usr/bin/python3`) や Homebrew版 (`/opt/homebrew/bin/python3`) の使用を**禁止**する。
- **Package Installer**: `uv pip` または `uv add/sync`
  - **禁止**: 素の `pip` コマンドの使用禁止（PATH汚染の原因）。
- **C++ Builder**: `CMake` (Homebrew版 / `/opt/homebrew/bin/cmake`)
  - **設定**: `CMakeLists.txt` にて `Python3_EXECUTABLE` を `.venv` 内に固定済み。

### 1.2 コマンド実行ルール
全てのPython関連コマンドは、以下のいずれかの方法で実行すること。
1. **uv run経由**: `uv run python script.py`
2. **Activate経由**: `source .venv/bin/activate` 後の `python script.py`

### 1.3 混ぜるな危険 (Environment Hygiene)
- **Homebrew**: `cmake`, `git` 等の**バイナリツール**のみ供給する。Pythonライブラリは供給しない。
- **User Local**: `~/.local` や `~/.pypirc` の設定に依存しないこと。

## 2. ハードウェア最適化 (Apple Silicon)
- **CPU/GPU**: M1 Mac (Apple Silicon) 専用。
- **Unified Memory**: 64GB 共有メモリを活用。
- **描画**: Metal API (Dear ImGui 経由) による GPU アクセラレーション。
- **推論コア**: MLX C++ API による Native 推論。

## 3. ディレクトリ構造 (Layer 3) - 2026-01-17 08:20 最新 (Actual)
- `/` (Root):
  - `.cursorrules` (Context Rules)
  - `.env`
  - `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`
  - `CMakeLists.txt` (Build Configuration)
  - `No*[1-4].md` (Protocols & Role Definitions)
  - `README.md`
  - `core/` (C++ Native Source: ASTParser, FileScanner, GraphVisualizer, PythonManager, main.mm)
  - `models/` (AI Models - Read Only)
  - `nodes/` (Schema Definitions - Empty)
  - `build/` (Artifacts - Excluded)
  - `.venv/` (Python Environment - Excluded)
  - `modules/` (Python Extensions: __init__.py)
  - `resources/` (Assets, Fonts)
  - `マイルストーン.md`, `インフラ設計.md`, `ユーザー設定.md`, `レポート.md`, `会話記録.md`
  - `作りたいもの.txt`, `修正完了報告.md`, `引き継ぎカルテ.md`, `憲法.md`, `設計図・世界観.md`
  - **MISSING**: `logic_miner.py` (Required by Protocol), `A.xlsx`

## 4. 通信・セキュリティ
- **ローカル完結**: 外部APIへの接続（OpenAI等）は明示的なユーザー設定がない限り禁止。
- **不変性**: モデルデータ（/models）は読み取り専用として扱い、スクリプトによる操作を制限する。