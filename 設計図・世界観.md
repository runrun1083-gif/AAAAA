
# ユーザーのやりたいこと
1つ目は、AIエージェント専用の設定画面。ノードを繋ぎ変えるだけで、AIにやらせたいことを自由自在にカスタマイズできるようにしたい。
で、2つ目はそれとは全く別物で、ノード上でプログラミング開発そのものをする機能なんだ。
例えば、GitHubにあるような優秀なコードのフォルダを読み込むと、ファイルの一つ一つが『箱』として画面に出てきて、ファイル同士の繋がりが『線』でパッと見える。それがテーブルの上に自動で広がっていくようなイメージだね。
その中身のコードを、一部でも全部でもC++に書き換えたり、JSON経由でC++化したりして、さっきのAI側のフレームワークと合体させたい。そうやって自分のアプリを開発していくことで、ややこしいライブラリや外部モジュールの依存から抜け出した、純粋で速いシステムを作りたいんだよね。」
# プロジェクト設計図・世界観 (Professional Edition)
1. エージェント・オーケストレーション・エンジン（AI制御）
AIエージェントの思考プロセスや行動トリガーを、ノードベースで直感的に、かつ精密に定義・変更する機能。

目的: 複雑なプロンプトやチェーン、ツールの呼び出し設定を可視化し、ユーザーの意図通りにエージェントを動かすための「司令塔」を構築する。

価値: 実装の詳細に踏み込むことなく、AIのキャラクターや推論ロジックを自由自在にチューニング可能とする。

2. ファイルシステム・ダイナミクス・開発エンジン（開発・統合）
ソースコード（GitHub等の既存プロジェクト）を解析し、視覚的なノード構造へと自動展開する「次世代の統合開発環境（IDE）」。

視覚的魔法: フォルダを指定すると、各ファイルが「コードを内包するノード」としてテーブル上に自動配置され、ファイル間の依存関係や呼び出し構造が「線（エッジ）」として可視化される。

C++への純粋化と統合:

読み込んだ既存コード（Python等）を一部、あるいは全部をC++へと書き換える（またはJSONを介してC++化する）プロセスを支援。

変換された高効率なロジックを、前述の「AIエージェントのフレームワーク」にシームレスに合体・統合する。

脱依存の実現: 肥大化したライブラリや不安定な外部モジュールへの依存を断ち切り、C++ネイティブで動作する「一級品」のスタンドアロン・アプリやモジュールを自ら生み出すための「開発ステーション」として機能する。
## 0. プロジェクトの方向性 (4要素定義)

### 1. 人物 (Persona)
プログラミング知識は少ないが、M1 Mac 64GB という強力な機材を所有し、その性能を極限まで引き出して「一級品」のAI体験を求めるパワーユーザー。

### 2. 状況 (Context)
高性能なローカル環境（Mac Studio/Pro）を活かしきれるツールが不足している。「使いやすいが低機能」でも「高機能だが難解」でもなく、富豪的リソースを背景にした「圧倒的スピード」と「直感的UI」が融合した環境を切望している。

### 3. アクション (Action)
Dear ImGuiベースの爆速Node UI上で、フォルダ内のファイルが自動展開・接続される魔法のような体験を享受する。MLX C++ APIによる最高速推論と、自分好みのプラグインを瞬時に追加・連携させる拡張アクション。

### 4. 制約 (Constraints)
- **手抜き禁止**: GILによるフリーズ、Retina非対応のボケ、ディスクアクセスによる遅延を一切許さないプロ仕様の実装。
- **リソース活用**: 64GB RAMをフル活用したインメモリASTキャッシュ層の構築。
- **環境徹底**: `uv.venv` 完全準拠、日本語徹底、透明性（承認必須）、純粋性（モデルデータ）。

---

## 1. ビジョン
「M1 Mac 64GB の暴力的リソースを背景に、Flowiseの概念を C++ / EnTT / MLX で再定義する、世界最速の個人用AI開発ステーション」

## 2. コアアーキテクチャ
- **GUI層**: Dear PyGui (HiDPI Retina対応)。ライブ・テレメトリによるリソース監視。
- **管理層**: EnTT (ECS) × インメモリASTキャッシュ。
- **推論層**: MLX C++ API (GPU直接活用)。
- **ビルド・基盤**: CMake (Python Embedding / Thread-safe Job Queue)。

## 3. 世界観・ユーザー体験
- **0ms Response**: RAMを活用した検索・解析の高速化。
- **Living Node**: フォルダを開くとファイルが意思を持つように広がり、依存関係が自動で結線される。
- **Sustainable Architecture**: スキーマ駆動により、未来のAIモデルもコード修正なしで統合可能。
