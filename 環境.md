インフラ設計ガイドライン
1. 開発・実行環境 (Layer 0) - 定義：完全分離型ハイブリッド
本プロジェクトは、システム環境(Homebrew/macOS)とプロジェクト環境(.venv)を厳格に分離する。

1.1 コア・ランタイム
Python Manager: uv (Universal Python Project Management)
役割: Pythonバージョンの管理、仮想環境の作成、パッケージの同期。
正義: pyproject.toml と uv.lock のみが真実である。
Python Runtime: .venv/bin/python (Python 3.12.12以上)
原則: システム標準 (/usr/bin/python3) や Homebrew版 (/opt/homebrew/bin/python3) の使用を禁止する。
Package Installer: uv pip または uv add/sync
禁止: 素の pip コマンドの使用禁止（PATH汚染の原因）。
C++ Builder: CMake (Homebrew版 / /opt/homebrew/bin/cmake)
設定: C++ Native App としてビルド。Python Embeddingが必要な場合のみ .venv 内のライブラリをリンク。
1.2 コマンド実行ルール
全てのPython関連コマンドは、以下のいずれかの方法で実行すること。

uv run経由: uv run python script.py
Activate経由: source .venv/bin/activate 後の python script.py
1.3 混ぜるな危険 (Environment Hygiene)
Homebrew: cmake, git 等のバイナリツールのみ供給する。Pythonライブラリは供給しない。
User Local: ~/.local や ~/.pypirc の設定に依存しないこと。
2. ハードウェア最適化 (Apple Silicon)
CPU/GPU: M1 Mac (Apple Silicon) 専用。
Unified Memory: 64GB 共有メモリを活用。
描画: Metal API (Dear ImGui 経由) による GPU アクセラレーション。
推論コア: MLX C++ API による Native 推論。
3. ディレクトリ構造 